# Housekeeping Console Plugin

This plugin exposes the existing `_housekeeping` console inside QiLabs Toolbox.

It launches:

```txt
C:\QiLabs\00_QiLabs.workspace\toolbox\_housekeeping\housekeeping_ui.py
```

The housekeeping UI stays separate because it already has its own preview/apply/undo workflow and safety model.