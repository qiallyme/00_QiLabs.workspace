      plugin_id: system.scheduler
      tool_id: system.scheduler
      name: Scheduler
      category: system
      version: 1.0.0
      enabled: true
      description: Loads and manages scheduled task queues for QiLabs automation. Supports cron-style scheduling and on-demand queue processing.
      tags:
        - system
- connector
- scheduler
- queue
- automation
      entry:
        type: plugin_v2
        target: scheduler.py
        class_name: QiPlugin
        working_dir: .
        open_console: false
      launch:
        type: python
        target: scheduler.py
        working_dir: .
        open_console: true
      module:
        package: tools.system.scheduler
        file: scheduler.py
        class_name: SchedulerTool
        entrypoint: SchedulerTool
      toolbox:
        ui_enabled: true
        bucket: system
        category: system
        launch_surface:
          - QiLabs Toolbox
        requires_target_directory: false
      runtime:
        requires_network: false
        requires_secrets: false
      safety:
        supports_dry_run: true
        supports_live_run: true
        deletes_files: false
        moves_files: false
        modifies_files: true
        review_before_live: true
      paths:
        source: tools/system/scheduler/scheduler.py
        readme: tools/system/scheduler/README.md
      status: migrated
      migrated_from: _archive
